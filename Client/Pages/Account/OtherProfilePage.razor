@page "/user/{id}"

<MudText>Profile</MudText>

@code {

    [Parameter]
    public string Id { get; set; }

    bool _userView;

    protected override void OnInitialized()
    {
        if (!string.IsNullOrEmpty(Id) && Guid.TryParse(Id, out var foo))
            _userView = true;
    }

}